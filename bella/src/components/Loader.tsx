import {RingLoader} from "react-spinners";

const Loader = () => {
    return (
        <div className="Loader">
            <RingLoader color="#555"/>
        </div>
    );
};

export default Loader;